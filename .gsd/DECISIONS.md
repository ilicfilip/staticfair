# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Static site framework | Astro (static output, TypeScript) | Content-heavy, mostly static site. Astro ships zero JS by default, has content collections, built-in image optimization | No |
| D002 | M001 | arch | CSS approach | Tailwind CSS with CSS custom properties for brand tokens | Light refresh with modern utility-first framework. Custom properties allow brand token changes without rebuilding | No |
| D003 | M001 | arch | Font | Mona Sans (self-hosted variable font) | Carries forward from current WP theme. Open source (MIT, GitHub). Variable font = single file for all weights | No |
| D004 | M001 | arch | Deployment target | Cloudflare Pages (pure static, no SSR adapter) | No dynamic features needed after excluding packages. Static output simplest to deploy and cache | Yes — if future feature requires SSR |
| D005 | M001 | scope | /packages/* handling | Excluded from Astro site. Served by standalone WP install via Cloudflare origin routing | Package explorer is most complex piece. Separate origin allows independent lifecycle | No |
| D006 | M001 | arch | Content repository strategy | Single repo (code + content together) | Simpler deploys, no sync issues. Content structure abstracted enough to split later | Yes — if content governance needs separation |
| D007 | M001 | arch | Content workflow | GitHub PRs, auto-deploy on merge to main | All contributors are technical. No CMS needed now | Yes — if non-technical editors join |
| D008 | M001 | arch | Blog content source | WordPress REST API extraction (one-time) | API is publicly accessible, returns full HTML content. Cleaner than WXR export parsing | No |
| D009 | M001 | scope | Branding | Placeholder text logo + favicon (designed for easy replacement) | Real branding hasn't been decided yet. Placeholders unblock launch | Yes — when brand decisions are made |
| D010 | M001 | convention | Blog URL structure | /blog/[slug]/ (drop date segments from WordPress URLs) | Cleaner URLs, better for sharing. Dates shown in post metadata, not URL. All old URLs get 301 redirects | No |
| D011 | M001/S01 | arch | Tailwind version | Tailwind CSS v4 via `@tailwindcss/vite` with CSS-first `@theme` configuration | Astro 6.x has first-class v4 support. CSS-first config is cleaner — no `tailwind.config.mjs` needed. `@theme` directive for brand tokens | No |
| D012 | M001/S01 | arch | Font loading strategy | Manual `@font-face` in global.css + preload link in BaseLayout | Simpler and more portable than Astro 6 Font API. Single variable font doesn't justify the API overhead. Preload with crossorigin for performance | Yes — if Font API proves beneficial later |
| D013 | M001/S01 | arch | SEO component approach | Custom `SEO.astro` component (not `astro-seo` package) | Simple enough to hand-build. Custom component gives full control over the S01→S04 contract. Props interface doubles as documentation for what every page must provide | No |
| D014 | M001/S01 | convention | Project directory | `src-astro/` subdirectory within repo root | Keeps Astro project cleanly separated from GSD artifacts and research docs at repo root | Yes — if repo structure changes |
| D015 | M001/S01 | arch | Inline @font-face in BaseLayout | `<style is:inline>` with @font-face in BaseLayout `<head>` | Ensures Mona Sans declaration appears in HTML output (not just compiled CSS bundle). Fixes V3 verification and improves critical font loading by avoiding render-blocking external CSS for font declaration | No |
| D016 | M001/S01 | convention | SEO canonical URL default | `new URL(Astro.url.pathname, Astro.site)` computes canonical automatically | Pages don't need to pass canonicalURL explicitly — it's derived from the current path and configured site URL. Produces `https://fair.pm/` in builds | No |
| D017 | M001/S01 | convention | BaseLayout props pass-through | BaseLayout accepts same Props interface as SEO.astro and forwards them | Pages set SEO props once on BaseLayout instead of importing and wiring SEO separately. Reduces boilerplate for every new page | No |
| D018 | M001/S02 | design | WCAG AA green replacement | `#1a7f53` replaces `#25b372` as the brand green token | Same hue (152.5°), 4.99:1 on white (passes AA), 4.62:1 on light-gray (passes AA). Closest AA-compliant match to original green | No |
| D019 | M001/S02 | arch | Mobile nav interactivity | Vanilla `<script>` in Header.astro for hamburger toggle — no framework hydration | Astro ships zero JS by default. Single `<script>` tag handles toggle, Escape key, and outside-click. No React/Vue needed | Yes — if view transitions require re-init |
| D020 | M001/S02 | arch | Desktop dropdown pattern | CSS-only `:hover` + `:focus-within` for desktop sub-menus | No JS needed for desktop dropdowns. Focus-within ensures keyboard accessibility. Mobile sub-menus display inline (no toggle) | Yes — if animation requirements change |
| D021 | M001/S02 | arch | Dark background focus indicators | `data-focus-dark` attribute + CSS override for white focus rings | Blue focus ring invisible on blue/dark-blue backgrounds. Dark sections set `data-focus-dark` and descendant focus rings switch to white outline | No |
| D022 | M001/S03 | arch | Code of Conduct content source | Use full WP content (21K chars) instead of website-content repo's 2-paragraph intro | The repo version is just a brief intro + link to GitHub. Using WP's full content ensures the page has real value rather than just a redirect | No |
| D023 | M001/S03 | arch | Blog image location strategy | Blog images in `src/assets/images/blog/` for Astro optimization; video in `public/video/` | Images in src/assets/ get automatic WebP/AVIF conversion, responsive srcset, width/height. Video goes in public/ because Astro doesn't optimize video | No |
| D024 | M001/S03 | arch | Embed handling in Markdown | Raw HTML in `.md` files with responsive CSS wrapper (not MDX) | Only ~7 embeds across 3 posts. Raw HTML in Markdown is simpler than converting to MDX. `.embed-responsive` CSS class provides 16:9 wrapper | Yes — if embed count grows significantly |
| D025 | M001/S03 | convention | Static pages as .astro files | Each static page is a standalone `.astro` file (not content collection) | Pages have unique layout needs and don't benefit from collection schema. `.astro` files give full control over structure and styling per page | No |
| D026 | M001/S03 | convention | RSS feed scope | Title, description, pubDate, and link only — no full post HTML in RSS | Simpler implementation, avoids markdown-it + sanitize-html dependency. RSS readers follow the link for full content | Yes — if full-content RSS is requested |
| D027 | M001/S03 | convention | WP-to-Astro block conversion | WP group blocks → `<aside>` with Tailwind; WP solution headings → `h4.text-blue`; strip all `wp-block-*` / `is-layout-*` classes; preserve `id` attributes for deep linking | Preserves semantic structure while removing WordPress cruft. `<aside>` is correct semantic element for callout/infobox content | No |
| D028 | M001/S03 | arch | Astro 6.x render() API for blog posts | `import { render } from 'astro:content'`, destructure `{ Content }`, render via `<Content />` component | This is the Astro 6.x pattern — replaces older `entry.render()` method. Used in [...slug].astro dynamic route | No |
| D029 | M001/S04 | arch | JSON-LD structured data helper pattern | `src/utils/structured-data.ts` with typed helper functions (`buildWebSite`, `buildOrganization`, `buildBlogPosting`, `buildBreadcrumbs`, `buildWebPage`, `buildGraph`). Each returns a plain object; `buildGraph` wraps in `@context` + `@graph`. Pages import helpers and pass result as `jsonLd` prop. | Keeps pages clean, ensures consistency across schema types, single source of truth for schema structure. No npm dependency needed — SEO.astro already renders via `JSON.stringify` | No |
| D030 | M001/S04 | convention | Default OG image as SVG | `public/og-default.svg` (1200×630, dark-blue background, white FAIR wordmark). Used as fallback when no page-specific image exists. SVG served directly — PNG conversion recommended for maximum Facebook compatibility but not blocking | Most social platforms accept SVG. Simplest creation path. Can be replaced with raster format later | Yes — if Facebook sharing tests show SVG not rendering |
| D031 | M001/S04 | convention | BlogPosting author fallback | Blog posts with no `author` frontmatter field default to `'FAIR'` in the BlogPosting JSON-LD schema | Most FAIR blog posts don't have named authors. Structured data requires author field. Using org name as fallback avoids empty fields while being accurate | Yes — if individual author attribution is added |
| D032 | M001/S04 | convention | Breadcrumb naming convention | BreadcrumbList items use short nav-level labels (e.g. 'TSC' not 'Technical Steering Committee', 'Working Groups' not 'FAIR Working Groups'). WebPage `name` uses readable page titles distinct from full `<title>` tag | Matches site navigation hierarchy. Short breadcrumb labels improve readability in SERPs. WebPage name is the page's semantic identity, separate from the formatted title tag | No |
